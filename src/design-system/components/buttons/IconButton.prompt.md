Icon-only square control — toolbar actions, table row actions, comm-box controls. Always pass `label` for accessibility.

```jsx
<IconButton label="Filter" variant="secondary"><FilterIcon/></IconButton>
<IconButton label="Close" variant="ghost"><XIcon/></IconButton>
```
