Text input with cockpit-label heading and blue focus glow. Use `mono` + `prefix` for codes.

```jsx
<Input label="Obligation ID" mono placeholder="BCAS-AEP-00" />
<Input label="Airport" prefix={<SearchIcon/>} error="Required" />
```
