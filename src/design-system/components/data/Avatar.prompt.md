Identity avatar for users/officers. Derives initials from `name`; deterministic tint; optional `status` dot.

```jsx
<Avatar name="R. Iyer" />
<Avatar name="Priya Nair" status="clear" size="lg" />
```
