Instrument-panel surface. Compose any content; set `interactive` for clickable cards (hover lift).

```jsx
<Card><h3>Departments cleared</h3>…</Card>
<Card interactive padding="var(--space-5)">…</Card>
```
