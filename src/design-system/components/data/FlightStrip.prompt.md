The signature CompliantBharat unit — one obligation per strip. Compact row by default; pass `detail` to make it expand (flap-deployment motion). The leading rule carries the regulator livery.

```jsx
<FlightStrip
  code="BCAS-AEP-11" regulator="BCAS" status="stop"
  title="Airport Entry Permit register reconciliation" due="OVERDUE 3D"
  department="Aerodrome Security" defaultExpanded
  detail="Quarterly reconciliation of all active AEPs against employment records…" />
```
